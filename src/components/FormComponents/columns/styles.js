
import {StyleSheet} from  'react-native';

const styles = StyleSheet.flatten({
  columns: {
    flex: 1,
    flexDirection: 'row',
    borderBottomWidth: 1,
    paddingVertical: 20,
  },
});

export default styles;
